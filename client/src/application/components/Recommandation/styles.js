import { makeStyles } from "@mui/styles";

export const useStyles = makeStyles({
  div: {
    marginTop: 20,
    height: 500,
    width: "100%",
    display: "flex",
    justifyContent: "space-evenly",
    alignItems: "center",
  },
});
