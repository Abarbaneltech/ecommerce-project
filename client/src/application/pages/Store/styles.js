import { makeStyles } from "@mui/styles";

export const useStyles = makeStyles({
  root: {
    // flexGrow: 1,
  },
  container: {
    display: "flex",
    justifyContent: "center",
    alignItems: "center",
  },
});
